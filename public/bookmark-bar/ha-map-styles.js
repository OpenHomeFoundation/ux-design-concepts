// Stub replacement. The original vector-map style definitions were removed
// from the project. Kept as a no-op global so the map page degrades gracefully
// rather than 404-ing on load.
window.HA_MAP_STYLES = window.HA_MAP_STYLES || {};
