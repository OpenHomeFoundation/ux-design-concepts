/* AUTO-GENERATED build artifact — compiled from changes-tray.jsx.
   Do not edit this file by hand. Edit changes-tray.jsx and rebuild. */

/* Changes tray — community-edit increment "changes-tray" (Stage 3, "B").

   A tray in the top bar, beside the notifications inbox, that gathers a
   contributor's unsubmitted DRAFTS across every device into one place. From
   here they can jump back into any device, discard a device's draft, or
   submit everything at once.

   Submitting from the tray fans out one device at a time: each drafted device
   becomes its own pending edit and opens its own pull request (openPullRequest
   in notifications.jsx). One device = one PR holds, exactly as in the
   per-device submit flow. The tray just batches the gesture.

   It also shows, read-only, what the contributor already has in review, so
   "your changes" is the single answer to "what have I got in flight?".

   Surfaces mirror the inbox: an anchored popover on desktop, a full-screen
   sheet on mobile/tablet (≤1023px). It reuses the inbox's panel/button
   classes for visual parity, with ce-tray-* classes for the row + footer.

   Gated by [data-inc-target="changes-tray"] on the nav wrapper, and returns
   null when signed out (drafts still work per-device for guests; the tray is
   the signed-in aggregate view, like the inbox). */

// Device thumbnail for a tray row: first product photo, else placeholder, else
// the category glyph. Mirrors the inbox thumb treatment.
function TrayThumb({
  device
}) {
  const photos = device && device.photos || [];
  const first = photos.length ? photos[0] : null;
  const isUrl = typeof first === 'string' && !first.startsWith('placeholder:');
  return /*#__PURE__*/React.createElement("span", {
    className: 'ce-inbox-thumb' + (first && isUrl ? ' has-photo' : ''),
    "aria-hidden": "true"
  }, first && isUrl ? /*#__PURE__*/React.createElement("img", {
    src: first,
    alt: "",
    loading: "lazy"
  }) : first && window.PhotoPlaceholder ? /*#__PURE__*/React.createElement(window.PhotoPlaceholder, {
    id: first + ':' + device.id + ':0',
    category: device.category,
    alt: ""
  }) : window.CategoryGlyph ? /*#__PURE__*/React.createElement(window.CategoryGlyph, {
    category: device.category,
    size: 22
  }) : null);
}
function ChangesTray() {
  const user = window.useCurrentUser && window.useCurrentUser() || null;
  window.useDrafts && window.useDrafts(); // re-render when drafts change
  window.usePending && window.usePending('__tray__'); // re-render when pending changes

  const [open, setOpen] = React.useState(false);
  const [fs, setFs] = React.useState(false);
  const [view, setView] = React.useState('list'); // 'list' | 'confirm'
  const [comment, setComment] = React.useState('');
  const [flash, setFlash] = React.useState(null); // { devices, changes } after a submit
  const [discardId, setDiscardId] = React.useState(null);
  const wrapRef = React.useRef(null);
  const matchesFs = () => typeof window !== 'undefined' && window.matchMedia('(max-width: 1023px)').matches;
  const openTray = () => {
    setFs(matchesFs());
    setView('list');
    setOpen(true);
  };
  const close = () => {
    setOpen(false);
    setView('list');
  };

  // Close on outside click (desktop popover only).
  React.useEffect(() => {
    if (!open || fs) return;
    const onDoc = e => {
      if (wrapRef.current && !wrapRef.current.contains(e.target)) close();
    };
    document.addEventListener('mousedown', onDoc);
    return () => document.removeEventListener('mousedown', onDoc);
  }, [open, fs]);

  // Close on Escape and on navigation.
  React.useEffect(() => {
    if (!open) return;
    const onKey = e => {
      if (e.key === 'Escape') close();
    };
    const onHash = () => close();
    window.addEventListener('keydown', onKey);
    window.addEventListener('hashchange', onHash);
    return () => {
      window.removeEventListener('keydown', onKey);
      window.removeEventListener('hashchange', onHash);
    };
  }, [open]);

  // Track the breakpoint while open so popover ↔ sheet swaps live.
  React.useEffect(() => {
    if (!open) return;
    const mq = window.matchMedia('(max-width: 1023px)');
    const onChange = e => setFs(e.matches);
    mq.addEventListener ? mq.addEventListener('change', onChange) : mq.addListener(onChange);
    return () => {
      mq.removeEventListener ? mq.removeEventListener('change', onChange) : mq.removeListener(onChange);
    };
  }, [open]);

  // Lock body scroll while the mobile sheet is open.
  React.useEffect(() => {
    if (open && fs) {
      document.body.classList.add('ce-inbox-sheet-open');
      return () => document.body.classList.remove('ce-inbox-sheet-open');
    }
  }, [open, fs]);
  if (!user) return null;

  // ── Gather drafts + in-review, across every device ──
  const devices = typeof window !== 'undefined' && window.DEVICES || [];
  const draftsMap = window.allDrafts ? window.allDrafts() : {};
  const draftItems = Object.keys(draftsMap).map(id => {
    const d = devices.find(x => x.id === id);
    const fields = draftsMap[id] && draftsMap[id].fields || {};
    const keys = Object.keys(fields);
    if (!d || keys.length === 0) return null;
    return {
      device: d,
      deviceId: id,
      count: keys.length,
      at: draftsMap[id].at
    };
  }).filter(Boolean).sort((a, b) => new Date(b.at) - new Date(a.at));
  const reviewItems = devices.map(d => {
    const p = window.getPending ? window.getPending(d.id) : {};
    const keys = Object.keys(p).filter(k => {
      const by = p[k] && p[k].by;
      return !by || by === window.CURRENT_USER;
    });
    if (keys.length === 0) return null;
    const at = keys.map(k => p[k].at).filter(Boolean).sort().reverse()[0];
    return {
      device: d,
      deviceId: d.id,
      count: keys.length,
      at
    };
  }).filter(Boolean).sort((a, b) => new Date(b.at) - new Date(a.at));
  const draftDevices = draftItems.length;
  const totalChanges = draftItems.reduce((n, it) => n + it.count, 0);
  const needsContact = window.hasNotificationMethod ? !window.hasNotificationMethod(user) : !(user.email || user.gh);
  const goDevice = id => {
    close();
    window.location.hash = '#/device/' + id;
  };
  const submitAll = () => {
    const devicesCount = draftItems.length;
    const changesCount = totalChanges;
    draftItems.forEach(it => {
      const entry = draftsMap[it.deviceId] || {};
      const fields = entry.fields || {};
      const sources = entry.sources || null;
      const entries = {};
      Object.keys(fields).forEach(k => {
        entries[k] = {
          value: fields[k]
        };
        if (sources && sources[k]) entries[k].source = sources[k];
      });
      if (window.addPendingEdit) window.addPendingEdit(it.deviceId, entries);
      if (window.openPullRequest) window.openPullRequest(it.device);
      if (window.clearDraft) window.clearDraft(it.deviceId);
    });
    setComment('');
    setFlash({
      devices: devicesCount,
      changes: changesCount
    });
    setView('list');
  };

  // ── Panel body ──
  const listBody = /*#__PURE__*/React.createElement(React.Fragment, null, flash && /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-flash",
    role: "status"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-flash-ico",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "checkcircle",
    size: 18
  })), /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-flash-text"
  }, /*#__PURE__*/React.createElement("b", null, flash.changes, " edit", flash.changes === 1 ? '' : 's', " submitted across ", flash.devices, " device", flash.devices === 1 ? '' : 's', "."), /*#__PURE__*/React.createElement("span", null, flash.devices, " pull request", flash.devices === 1 ? '' : 's', " opened. Follow each one from your inbox.")), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ce-tray-flash-x",
    "aria-label": "Dismiss",
    onClick: () => setFlash(null)
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "x",
    size: 15
  }))), draftDevices === 0 && reviewItems.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "ce-inbox-empty"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "checkcircle",
    size: 22
  }), /*#__PURE__*/React.createElement("p", null, "No edits in progress.")) : /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-scroll"
  }, draftDevices > 0 && /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-group"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-group-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-group-title"
  }, "Drafts")), /*#__PURE__*/React.createElement("ul", {
    className: "ce-tray-list"
  }, draftItems.map(it => /*#__PURE__*/React.createElement("li", {
    key: it.deviceId,
    className: "ce-tray-row"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ce-tray-row-main",
    onClick: () => goDevice(it.deviceId)
  }, /*#__PURE__*/React.createElement(TrayThumb, {
    device: it.device
  }), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-body"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-name"
  }, it.device.name), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-meta"
  }, it.count, " edit", it.count === 1 ? '' : 's', " \xB7 ", it.device.manufacturer))), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-actions"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ce-tray-rowbtn ce-tray-rowbtn-danger",
    "aria-label": 'Discard draft for ' + it.device.name,
    onClick: () => setDiscardId(it.deviceId)
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "trash",
    size: 14
  }))))))), reviewItems.length > 0 && /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-group"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-group-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-group-title"
  }, "Awaiting review")), /*#__PURE__*/React.createElement("ul", {
    className: "ce-tray-list"
  }, reviewItems.map(it => /*#__PURE__*/React.createElement("li", {
    key: it.deviceId,
    className: "ce-tray-row ce-tray-row-review"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ce-tray-row-main",
    onClick: () => goDevice(it.deviceId)
  }, /*#__PURE__*/React.createElement(TrayThumb, {
    device: it.device
  }), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-body"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-name"
  }, it.device.name), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-meta"
  }, it.count, " edit", it.count === 1 ? '' : 's', " awaiting review")))))))), draftDevices > 0 && /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-foot"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn btn-primary ce-tray-submit",
    onClick: () => {
      setFlash(null);
      setView('confirm');
    }
  }, "Submit all")));
  const confirmBody = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-confirm"
  }, /*#__PURE__*/React.createElement("p", {
    className: "ce-tray-confirm-lede"
  }, "Submitting ", /*#__PURE__*/React.createElement("b", null, totalChanges, " edit", totalChanges === 1 ? '' : 's'), " across ", /*#__PURE__*/React.createElement("b", null, draftDevices, " device", draftDevices === 1 ? '' : 's'), ". This opens ", /*#__PURE__*/React.createElement("b", null, draftDevices, " pull request", draftDevices === 1 ? '' : 's'), ", one per device, reviewed on GitHub before going live."), /*#__PURE__*/React.createElement("ul", {
    className: "ce-tray-confirm-list"
  }, draftItems.map(it => /*#__PURE__*/React.createElement("li", {
    key: it.deviceId,
    className: "ce-tray-confirm-item"
  }, /*#__PURE__*/React.createElement(TrayThumb, {
    device: it.device
  }), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-confirm-body"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-name"
  }, it.device.name), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-row-meta"
  }, it.count, " edit", it.count === 1 ? '' : 's')), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-confirm-pr"
  }, "1 PR")))), needsContact && /*#__PURE__*/React.createElement("div", {
    className: "ce-notify-warning",
    role: "note"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "info",
    size: 16
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("b", null, "No notification method set up."), " Add a GitHub account or an email so we can tell you when these are reviewed.", ' ', /*#__PURE__*/React.createElement("a", {
    href: "#/settings/notifications",
    onClick: close
  }, "Set one up"), ".")), /*#__PURE__*/React.createElement("label", {
    className: "ce-field ce-tray-comment",
    htmlFor: "ce-tray-comment"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-field-label"
  }, "Comment"), /*#__PURE__*/React.createElement("textarea", {
    id: "ce-tray-comment",
    className: "ce-textarea",
    rows: 3,
    placeholder: "What did you change and why? Added to every pull request.",
    value: comment,
    onChange: e => setComment(e.target.value)
  }))), /*#__PURE__*/React.createElement("div", {
    className: "ce-tray-foot ce-tray-foot-confirm"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn btn-ghost",
    onClick: () => setView('list')
  }, "Back"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn btn-primary",
    onClick: submitAll
  }, "Submit for review")));
  const title = view === 'confirm' ? 'Submit your edits' : 'Your edits';
  const body = view === 'confirm' ? confirmBody : listBody;
  const button = /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: 'ce-inbox-btn' + (open ? ' is-open' : ''),
    "aria-label": draftDevices ? `Your edits, ${draftDevices} device${draftDevices === 1 ? '' : 's'} with drafts` : 'Your edits',
    "aria-haspopup": "true",
    "aria-expanded": open,
    onClick: () => open ? close() : openTray()
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "edit",
    size: 19
  }), draftDevices > 0 && /*#__PURE__*/React.createElement("span", {
    className: "ce-inbox-badge",
    "aria-hidden": "true"
  }, draftDevices > 9 ? '9+' : draftDevices));
  return /*#__PURE__*/React.createElement("span", {
    className: "ce-inbox ce-tray",
    "data-inc-target": "changes-tray",
    ref: wrapRef
  }, button, open && !fs && /*#__PURE__*/React.createElement("div", {
    className: "ce-inbox-pop ce-tray-pop",
    role: "dialog",
    "aria-label": title
  }, /*#__PURE__*/React.createElement("div", {
    className: "ce-inbox-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-inbox-title"
  }, title)), body), open && fs && ReactDOM.createPortal(/*#__PURE__*/React.createElement("div", {
    className: "ce-inbox-fs ce-tray-fs",
    role: "dialog",
    "aria-label": title
  }, /*#__PURE__*/React.createElement("div", {
    className: "ce-inbox-fs-bar"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ce-inbox-fs-back",
    "aria-label": view === 'confirm' ? 'Back' : 'Close',
    onClick: () => view === 'confirm' ? setView('list') : close()
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "arrowL",
    size: 20
  })), /*#__PURE__*/React.createElement("span", {
    className: "ce-inbox-fs-title"
  }, title), /*#__PURE__*/React.createElement("span", {
    className: "ce-inbox-fs-spacer"
  })), /*#__PURE__*/React.createElement("div", {
    className: "ce-inbox-fs-body ce-tray-fs-body"
  }, body)), document.body), window.ConfirmDialog && /*#__PURE__*/React.createElement(window.ConfirmDialog, {
    open: !!discardId,
    title: "Discard this draft?",
    message: "Your unsubmitted edits to this device will be lost. This can\u2019t be undone.",
    confirmLabel: "Discard draft",
    cancelLabel: "Keep draft",
    danger: true,
    onConfirm: () => {
      if (window.clearDraft) window.clearDraft(discardId);
      setDiscardId(null);
    },
    onCancel: () => setDiscardId(null)
  }));
}
Object.assign(window, {
  ChangesTray
});
