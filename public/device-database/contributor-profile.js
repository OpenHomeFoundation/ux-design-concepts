/* AUTO-GENERATED build artifact — compiled from contributor-profile.jsx.
   Do not edit this file by hand. Edit contributor-profile.jsx and rebuild. */

function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Public contributor profiles + the contributors directory.
   Community-edit increment.

   Routes (wired in app.jsx):
     · #/contributors            → <ContributorsIndex />   (directory)
     · #/contributors/:handle    → <ContributorProfile />  (one person)

   A profile is identity (avatar, name, bio, links) plus a contributions
   timeline. The SAME component renders your own profile and everyone
   else's; your own adds Settings / Sign out and is reached by clicking the
   nav avatar. Pending ("awaiting review") edits are public here, by product
   decision, so they show on every profile alongside live edits.

   The timeline is derived, not stored: live edits come from each device's
   editHistory filtered by author, pending edits from the pending store.

   Privacy: window.USERS[id].private hides the whole profile from others;
   .hideActivity keeps the header but hides stats + timeline. You always see
   your own in full.
*/

// ─────────────────────────────────────────────────────────────────────
// Field-key → human label (for pending edits, which are keyed by field).
// ─────────────────────────────────────────────────────────────────────
const PROFILE_FIELD_LABELS = {
  name: 'Name',
  summary: 'Summary',
  photos: 'Photos',
  description: 'Description',
  instructions: 'Setup instructions',
  ecosystems: 'Ecosystems and apps',
  connectivity: 'Connectivity',
  protocols: 'Protocols',
  bridge: 'Bridge',
  dimensions: 'Dimensions',
  identifiers: 'Identifiers',
  references: 'References',
  connectsWith: 'Connects with'
};
function _prettifyKey(k) {
  return String(k).replace(/([A-Z])/g, ' $1').replace(/^./, c => c.toUpperCase()).trim();
}
function profileFieldLabel(key) {
  if (!key) return 'Field';
  if (key.indexOf('spec:') === 0) {
    const k = key.slice(5);
    const specs = typeof window !== 'undefined' && window.CATEGORY_SPECS || {};
    for (const cat in specs) {
      const hit = (specs[cat] || []).find(f => f.key === k);
      if (hit && hit.label) return hit.label;
    }
    return _prettifyKey(k);
  }
  return PROFILE_FIELD_LABELS[key] || _prettifyKey(key);
}

// The auto "device created" provenance row is not a human edit, mirror the
// rule used by the detail page so the timeline only shows real edits.
function _isCreationEntry(e) {
  if (!e) return false;
  if (e.summary === 'Initial entry') return true;
  return Array.isArray(e.changes) && e.changes.length === 1 && e.changes[0] && e.changes[0].verb === 'created';
}

// Gather one contributor's edits across the whole database.
function gatherContributions(handle) {
  const devices = typeof window !== 'undefined' && window.DEVICES || [];
  const live = [];
  devices.forEach(d => {
    (d.editHistory || []).forEach(e => {
      if (e.by !== handle || _isCreationEntry(e)) return;
      live.push({
        kind: 'live',
        deviceId: d.id,
        device: d,
        at: e.at,
        pr: e.pr,
        summary: e.summary,
        changes: e.changes || []
      });
    });
  });
  const pending = [];
  devices.forEach(d => {
    const p = window.getPending && window.getPending(d.id) || {};
    const mine = Object.keys(p).filter(k => p[k] && p[k].by === handle);
    if (mine.length === 0) return;
    const at = mine.map(k => p[k].at).filter(Boolean).sort().reverse()[0];
    const pr = p[mine[0]].pr;
    pending.push({
      kind: 'pending',
      deviceId: d.id,
      device: d,
      at,
      pr,
      changes: mine.map(k => ({
        field: k,
        verb: 'edited'
      }))
    });
  });
  const all = [...live, ...pending].sort((a, b) => new Date(b.at) - new Date(a.at));
  // Contributions are submissions (pull requests): each merged edit-event and
  // each in-review device is one PR. Counting fields on the in-review side
  // would mix units with the merged side, so we count PRs consistently.
  const contributions = live.length + pending.length;
  const deviceIds = new Set(all.map(c => c.deviceId));
  const since = all.length ? all.map(c => c.at).filter(Boolean).sort()[0] : null;
  return {
    live,
    pending,
    all,
    contributions,
    deviceCount: deviceIds.size,
    since
  };
}

// Your own unsubmitted drafts (Stage 3 "A"). These belong to your account
// and are never attributed to anyone publicly, so they show only on your own
// profile, and never count toward the public contribution totals.
function gatherDrafts() {
  const devices = typeof window !== 'undefined' && window.DEVICES || [];
  const drafts = window.allDrafts && window.allDrafts() || {};
  const items = [];
  Object.keys(drafts).forEach(id => {
    const d = devices.find(x => x.id === id);
    const fields = drafts[id] && drafts[id].fields || {};
    const keys = Object.keys(fields);
    if (!d || keys.length === 0) return;
    items.push({
      kind: 'draft',
      deviceId: id,
      device: d,
      at: drafts[id].at,
      changes: keys.map(k => ({
        field: k,
        verb: 'edited'
      }))
    });
  });
  return items;
}
function _monthYear(input) {
  if (!input) return '-';
  const d = new Date(input);
  if (isNaN(d)) return '—';
  return d.toLocaleDateString('en-GB', {
    month: 'short',
    year: 'numeric'
  });
}
function _hostname(url) {
  try {
    return new URL(url).hostname.replace(/^www\./, '');
  } catch (e) {
    return String(url).replace(/^https?:\/\//, '').replace(/^www\./, '').replace(/\/.*$/, '');
  }
}
function _faviconUrl(url) {
  const host = _hostname(url);
  return host ? `https://www.google.com/s2/favicons?sz=64&domain=${encodeURIComponent(host)}` : '';
}

// ─────────────────────────────────────────────────────────────────────
// One contribution row in the timeline. Device-forward (avatar is the
// device glyph, not the person, since the whole list is one person).
// ─────────────────────────────────────────────────────────────────────
function ContributionRow({
  item
}) {
  const Glyph = window.CategoryGlyph;
  const d = item.device;
  const when = window.formatDateShort ? window.formatDateShort(item.at) : item.at;
  const full = window.formatDateTime ? window.formatDateTime(item.at) : when;
  // Mirror the Browse card: prefer the device's first product photo, then a
  // styled placeholder, then the category glyph.
  const photos = d && d.photos || [];
  const firstPhoto = photos.length ? photos[0] : null;
  const isUrl = typeof firstPhoto === 'string' && !firstPhoto.startsWith('placeholder:');
  // Whole row is clickable. Use a JS handler rather than a stretched-link
  // overlay so the row's children stay individually selectable (design
  // markup tool, etc). Inner anchors handle their own clicks, and an active
  // text selection is left alone.
  const goToDevice = e => {
    if (e.target.closest('a')) return;
    if (window.getSelection && String(window.getSelection())) return;
    window.location.hash = `#/device/${item.deviceId}`;
  };
  return /*#__PURE__*/React.createElement("li", {
    className: "ce-contrib-row",
    onClick: goToDevice
  }, /*#__PURE__*/React.createElement("a", {
    className: 'ce-contrib-tile' + (firstPhoto && isUrl ? ' has-photo' : ''),
    href: `#/device/${item.deviceId}`,
    "aria-hidden": "true",
    tabIndex: -1
  }, firstPhoto && isUrl ? /*#__PURE__*/React.createElement("img", {
    src: firstPhoto,
    alt: "",
    loading: "lazy",
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'contain',
      display: 'block'
    }
  }) : firstPhoto && window.PhotoPlaceholder ? /*#__PURE__*/React.createElement(window.PhotoPlaceholder, {
    id: firstPhoto + ':' + d.id + ':0',
    category: d.category,
    alt: ""
  }) : Glyph ? /*#__PURE__*/React.createElement(Glyph, {
    category: d.category,
    size: 26
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-body"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-meta"
  }, /*#__PURE__*/React.createElement("time", {
    className: "ce-contrib-when",
    dateTime: item.at,
    title: full
  }, when), item.kind === 'pending' ? /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-badge ce-contrib-badge-pending"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-pending-dot",
    "aria-hidden": "true"
  }), " Awaiting review") : item.kind === 'draft' ? /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-badge ce-contrib-badge-draft"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-pending-dot ce-draft-dot",
    "aria-hidden": "true"
  }), " Draft") : null), /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-top"
  }, /*#__PURE__*/React.createElement("a", {
    className: "ce-contrib-device",
    href: `#/device/${item.deviceId}`
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-name"
  }, d.name), /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-mfr"
  }, d.manufacturer))), item.summary ? /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-summary"
  }, item.summary) : null, item.changes && item.changes.length > 0 && window.EditChangeLine ? /*#__PURE__*/React.createElement("ul", {
    className: "ce-change-list ce-contrib-changes"
  }, item.changes.map((c, i) => /*#__PURE__*/React.createElement(window.EditChangeLine, {
    key: i,
    change: c
  }))) : null));
}

// ─────────────────────────────────────────────────────────────────────
// Profile page
// ─────────────────────────────────────────────────────────────────────
function ContributorProfile({
  handle
}) {
  window.useCurrentUser && window.useCurrentUser(); // re-render on sign-out
  window.useDrafts && window.useDrafts(); // re-render when your local drafts change
  const user = window.getUser ? window.getUser(handle) : null;
  const currentId = window.CURRENT_USER;
  const isSelf = handle === currentId;
  if (!user) {
    return /*#__PURE__*/React.createElement("div", {
      className: "container ce-profile-missing",
      "data-screen-label": "Profile not found"
    }, /*#__PURE__*/React.createElement("h1", null, "Contributor not found."), /*#__PURE__*/React.createElement("p", {
      className: "muted"
    }, "We couldn't find a contributor with that handle."), /*#__PURE__*/React.createElement("a", {
      className: "btn btn-secondary",
      href: "#/contributors"
    }, "All contributors"));
  }
  const isPrivate = !!user.private && !isSelf;
  const activityHidden = !!user.hideActivity && !isSelf;

  // Centered empty-state for a fully-private profile.
  if (isPrivate) {
    return /*#__PURE__*/React.createElement("div", {
      className: "container ce-profile-private",
      "data-screen-label": "Profile (private)"
    }, /*#__PURE__*/React.createElement("div", {
      className: "ce-private-card"
    }, /*#__PURE__*/React.createElement("div", {
      className: "ce-private-avatar"
    }, /*#__PURE__*/React.createElement(window.Avatar, {
      user: user,
      size: 88
    }), /*#__PURE__*/React.createElement("span", {
      className: "ce-private-lock",
      "aria-hidden": "true"
    }, /*#__PURE__*/React.createElement(window.Icon, {
      name: "lock",
      size: 18,
      strokeWidth: 2.25
    }))), /*#__PURE__*/React.createElement("h1", {
      className: "ce-private-name"
    }, user.name), /*#__PURE__*/React.createElement("p", {
      className: "ce-private-headline"
    }, "This profile is private"), /*#__PURE__*/React.createElement("p", {
      className: "ce-private-sub"
    }, user.name, " has chosen to keep their profile and contribution history hidden."), /*#__PURE__*/React.createElement("a", {
      className: "btn btn-secondary ce-private-back",
      href: "#/contributors"
    }, "All contributors")));
  }
  const {
    all,
    contributions,
    deviceCount,
    since
  } = gatherContributions(handle);
  // Drafts never appear on the public profile, not even your own, they live
  // only on the private "Your changes" dashboard (#/you).
  const feed = all;
  const socials = Array.isArray(user.socials) ? user.socials.filter(s => s && s.url) : [];
  return /*#__PURE__*/React.createElement("div", {
    className: "container ce-profile",
    "data-screen-label": isSelf ? 'Your profile' : 'Contributor profile'
  }, isSelf && user.private ? /*#__PURE__*/React.createElement(window.Banner, {
    className: "ce-banner-at-profile",
    icon: "lock",
    title: "Your profile is private",
    actions: /*#__PURE__*/React.createElement("a", {
      className: "btn btn-secondary btn-sm",
      href: "#/settings"
    }, "Change visibility")
  }, "Only you can see this page. To everyone else it shows as a locked, hidden profile, and you are left out of the contributors directory.") : null, /*#__PURE__*/React.createElement("aside", {
    className: "ce-profile-aside"
  }, /*#__PURE__*/React.createElement(window.Avatar, {
    user: user,
    size: 96
  }), /*#__PURE__*/React.createElement("div", {
    className: "ce-profile-id"
  }, /*#__PURE__*/React.createElement("h1", {
    className: "ce-profile-name"
  }, user.name), user.pronouns ? /*#__PURE__*/React.createElement("div", {
    className: "ce-profile-handle"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-profile-pronouns"
  }, user.pronouns)) : null, user.bio ? /*#__PURE__*/React.createElement("p", {
    className: "ce-profile-bio"
  }, user.bio) : null, /*#__PURE__*/React.createElement("div", {
    className: "ce-profile-links"
  }, user.location ? /*#__PURE__*/React.createElement("div", {
    className: "ce-profile-link ce-profile-meta"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "pin",
    size: 14
  }), user.location) : null, user.url ? /*#__PURE__*/React.createElement("a", {
    className: "ce-profile-link",
    href: user.url,
    target: "_blank",
    rel: "noopener noreferrer"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "link",
    size: 14
  }), _hostname(user.url)) : null, user.gh ? /*#__PURE__*/React.createElement("a", {
    className: "ce-profile-link",
    href: `https://github.com/${user.gh}`,
    target: "_blank",
    rel: "noopener noreferrer"
  }, /*#__PURE__*/React.createElement(window.GitHubMark, {
    size: 14
  }), user.gh) : null, socials.map((s, i) => /*#__PURE__*/React.createElement("a", {
    key: i,
    className: "ce-profile-link",
    href: s.url,
    target: "_blank",
    rel: "noopener noreferrer"
  }, /*#__PURE__*/React.createElement("img", {
    className: "ce-profile-favicon",
    src: _faviconUrl(s.url),
    alt: "",
    width: 16,
    height: 16,
    loading: "lazy"
  }), _hostname(s.url))))), isSelf ? /*#__PURE__*/React.createElement("div", {
    className: "ce-profile-actions"
  }, /*#__PURE__*/React.createElement("a", {
    className: "btn btn-secondary ce-acct-photo-btn",
    href: "#/settings"
  }, "Edit profile")) : null, !activityHidden ? /*#__PURE__*/React.createElement("div", {
    className: "aside-card aside-stats ce-profile-stats-card"
  }, /*#__PURE__*/React.createElement("h4", null, "Activity"), /*#__PURE__*/React.createElement("dl", {
    className: "aside-stat-list"
  }, /*#__PURE__*/React.createElement("div", {
    className: "aside-stat"
  }, /*#__PURE__*/React.createElement("dt", null, "Contributions"), /*#__PURE__*/React.createElement("dd", null, contributions)), /*#__PURE__*/React.createElement("div", {
    className: "aside-stat"
  }, /*#__PURE__*/React.createElement("dt", null, "Devices touched"), /*#__PURE__*/React.createElement("dd", null, deviceCount)), /*#__PURE__*/React.createElement("div", {
    className: "aside-stat"
  }, /*#__PURE__*/React.createElement("dt", null, "Contributing since"), /*#__PURE__*/React.createElement("dd", null, _monthYear(since))))) : null), /*#__PURE__*/React.createElement("main", {
    className: "ce-profile-main"
  }, activityHidden ? /*#__PURE__*/React.createElement("div", {
    className: "ce-profile-activity-hidden"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "info",
    size: 18
  }), /*#__PURE__*/React.createElement("span", null, "This contributor has hidden their activity.")) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "ce-profile-feed"
  }, feed.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-empty"
  }, isSelf ? "You haven't contributed yet. Find a device and add what you know." : 'No contributions yet.', isSelf ? /*#__PURE__*/React.createElement("a", {
    className: "btn btn-secondary ce-contrib-empty-cta",
    href: "#/browse"
  }, "Browse devices") : null) : /*#__PURE__*/React.createElement("ol", {
    className: "ce-contrib-list"
  }, feed.map((item, i) => /*#__PURE__*/React.createElement(ContributionRow, {
    key: i,
    item: item
  })))))));
}

// ─────────────────────────────────────────────────────────────────────
// Contributors directory
// ─────────────────────────────────────────────────────────────────────
function ContributorsIndex() {
  window.useCurrentUser && window.useCurrentUser();
  const users = typeof window !== 'undefined' && window.USERS || {};
  const rows = Object.keys(users).map(id => {
    const u = users[id];
    const {
      contributions,
      deviceCount
    } = gatherContributions(id);
    return {
      id,
      user: u,
      contributions,
      deviceCount,
      isSelf: id === window.CURRENT_USER
    };
  })
  // Private contributors are hidden from the directory entirely. You always
  // see your own card (you-proof), marked so you know you're hidden.
  .filter(r => r.isSelf || !r.user.private)
  // Alphabetical by name so the A–Z grouping reads cleanly.
  .sort((a, b) => (a.user.name || '').localeCompare(b.user.name || '', undefined, {
    sensitivity: 'base'
  }));

  // Group into A–Z buckets; anything not starting with a letter falls under "#".
  const groups = [];
  const byLetter = {};
  rows.forEach(r => {
    const ch = ((r.user.name || '?').trim().charAt(0) || '?').toUpperCase();
    const letter = /[A-Z]/.test(ch) ? ch : '#';
    if (!byLetter[letter]) {
      byLetter[letter] = {
        letter,
        rows: []
      };
      groups.push(byLetter[letter]);
    }
    byLetter[letter].rows.push(r);
  });
  // "#" always sorts last, letters otherwise stay in encounter (already A→Z) order.
  groups.sort((a, b) => (a.letter === '#' ? 1 : 0) - (b.letter === '#' ? 1 : 0));
  return /*#__PURE__*/React.createElement("div", {
    className: "container ce-contributors",
    "data-screen-label": "Contributors"
  }, /*#__PURE__*/React.createElement("header", {
    className: "ce-contributors-head"
  }, /*#__PURE__*/React.createElement("h1", null, "Contributors"), /*#__PURE__*/React.createElement("p", {
    className: "muted"
  }, "A heartfelt thank you to the volunteers who keep this database accurate. Every contribution is attributed, and carefully reviewed before it goes live.")), /*#__PURE__*/React.createElement("div", {
    className: "ce-contributors-list"
  }, groups.map(g => /*#__PURE__*/React.createElement("section", {
    className: "ce-contributor-group",
    key: g.letter
  }, /*#__PURE__*/React.createElement("h2", {
    className: "ce-contributor-letter"
  }, g.letter), /*#__PURE__*/React.createElement("ul", {
    className: "ce-contributor-rows"
  }, g.rows.map(r => {
    return /*#__PURE__*/React.createElement("li", {
      key: r.id
    }, /*#__PURE__*/React.createElement("a", {
      className: "ce-contributor-row",
      href: `#/contributors/${r.id}`
    }, /*#__PURE__*/React.createElement(window.Avatar, {
      user: r.user,
      size: 40
    }), /*#__PURE__*/React.createElement("div", {
      className: "ce-contributor-main"
    }, /*#__PURE__*/React.createElement("div", {
      className: "ce-contributor-name"
    }, r.user.name, r.isSelf ? /*#__PURE__*/React.createElement("span", {
      className: "ce-contributor-you"
    }, "You") : null), /*#__PURE__*/React.createElement("div", {
      className: "ce-contributor-handle"
    }, r.user.location || '')), /*#__PURE__*/React.createElement("div", {
      className: "ce-contributor-count"
    }, r.isSelf && r.user.private ? /*#__PURE__*/React.createElement("span", {
      className: "ce-contributor-private"
    }, /*#__PURE__*/React.createElement(window.Icon, {
      name: "lock",
      size: 12
    }), " Private") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("b", null, r.contributions), " contribution", r.contributions === 1 ? '' : 's'))));
  }))))));
}
Object.assign(window, {
  ContributorProfile,
  ContributorsIndex,
  gatherContributions,
  gatherDrafts,
  profileFieldLabel
});
