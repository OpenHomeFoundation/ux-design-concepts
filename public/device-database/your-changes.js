/* AUTO-GENERATED build artifact — compiled from your-changes.jsx.
   Do not edit this file by hand. Edit your-changes.jsx and rebuild. */

/* "Your changes" — the private dashboard, as a sortable table.

   A single private surface that merges what used to be three separate
   surfaces: the notifications inbox, the changes tray, and the private
   (self) view of the contributor profile. It answers one question:
   "what have I got in flight, and what happened to it?"

   Layout is an inbox: a left folder rail with Drafts at the top (it is where
   you act), then a hairline, then the submitted lifecycle states
   All / Awaiting review / Live / Declined, each with a count (the All folder
   is labelled "Submitted"). Drafts are unsubmitted and account-only, a
   different class from the GitHub-tracked states, so Submitted does not include
   them. The page opens on Drafts when you have any, otherwise on Submitted. On
   tablet the rail collapses to a horizontal tab strip, and on mobile the table
   becomes cards.

   Lifecycle states:
     · Draft        unsubmitted, saved to your account. Continue or discard.
     · Pending      submitted, a pull request is open. Links to the PR.
     · Live         merged and public. Links to the device page.
     · Declined     not accepted. Can be revised (reopened on the device).

   "Submit all" batches every draft: one pull request per device.

   Gated by the your-changes increment. Returns a sign-in prompt when signed
   out. */

const YC_PR_BASE = 'https://github.com/OpenHomeFoundation/device-database/pull/';

// Lifecycle presentation. `order` drives the Status sort.
const YC_STATES = {
  draft: {
    label: 'Draft',
    badgeClass: 'yc-badge-draft',
    order: 0
  },
  pending: {
    label: 'Awaiting review',
    badgeClass: 'yc-badge-pending',
    order: 1
  },
  declined: {
    label: 'Declined',
    badgeClass: 'yc-badge-declined',
    order: 2
  },
  live: {
    label: 'Live',
    badgeClass: 'yc-badge-live',
    order: 3
  }
};

// PR comment lookup (the GitHub-API seam, seeded in data.js).
function ycPrMeta(pr) {
  return pr && window.PR_COMMENTS && window.PR_COMMENTS[pr] || null;
}

// Whole-row click. A draft opens straight into edit mode (no PR exists yet);
// a submitted change (pending / live / declined) opens its review process —
// the pull request — since that's where the change and its status live, and
// the device read page wouldn't show an in-flight or declined edit anyway.
// Ignores clicks on inner links/buttons and leaves a text selection alone.
function ycRowClick(item) {
  return e => {
    if (e.target.closest('a, button')) return;
    if (window.getSelection && String(window.getSelection())) return;
    if (item.kind === 'draft') {
      window.__ceEditIntent = item.deviceId;
      window.location.hash = '#/device/' + item.deviceId;
      return;
    }
    if (item.pr) {
      window.open(YC_PR_BASE + item.pr, '_blank', 'noopener');
      return;
    }
    window.location.hash = '#/device/' + item.deviceId;
  };
}

// Build rich change lines (field + verb + before→after where the value is a
// short scalar) from a map of { fieldKey: newValue }. Reuses describeChanges,
// the exact diff logic the live submit flow uses, so a draft or pending edit
// shows the same detail it will once it's live. Complex values (objects,
// arrays, long text) render as a plain "Updated <field>" line, same as
// everywhere else in the app.
function ycRichChanges(device, valueMap) {
  if (!device || !valueMap) return [];
  const keys = Object.keys(valueMap);
  if (!keys.length) return [];
  if (!window.describeChanges || !window.setField) {
    return keys.map(k => ({
      field: k,
      verb: 'edited'
    }));
  }
  let after = {
    ...device
  };
  keys.forEach(k => {
    after = window.setField(after, k, valueMap[k]);
  });
  return window.describeChanges(device, after, keys);
}

// ── Device thumbnail used in the table + cards. Mirrors the Browse card /
//    contribution-row tile exactly: flush category glyph by default, and a
//    white-filled rounded box (no hairline) once there's a real photo. ──
function YctThumb({
  device,
  size = 40
}) {
  const Glyph = window.CategoryGlyph;
  const photos = device && device.photos || [];
  const first = photos.length ? photos[0] : null;
  const isUrl = typeof first === 'string' && !first.startsWith('placeholder:');
  return /*#__PURE__*/React.createElement("span", {
    className: 'yct-thumb' + (first && isUrl ? ' has-photo' : ''),
    "aria-hidden": "true"
  }, first && isUrl ? /*#__PURE__*/React.createElement("img", {
    src: first,
    alt: "",
    loading: "lazy",
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'contain',
      display: 'block'
    }
  }) : first && window.PhotoPlaceholder ? /*#__PURE__*/React.createElement(window.PhotoPlaceholder, {
    id: first + ':' + device.id + ':0',
    category: device.category,
    alt: ""
  }) : Glyph ? /*#__PURE__*/React.createElement(Glyph, {
    category: device.category,
    size: size === 40 ? 24 : size - 12
  }) : null);
}

// Compact thumbnail used inside the submit-all modal list (kept from the
// previous tray design).
function YcTile({
  device
}) {
  const Glyph = window.CategoryGlyph;
  const photos = device && device.photos || [];
  const first = photos.length ? photos[0] : null;
  const isUrl = typeof first === 'string' && !first.startsWith('placeholder:');
  return /*#__PURE__*/React.createElement("span", {
    className: 'ce-contrib-tile' + (first && isUrl ? ' has-photo' : ''),
    "aria-hidden": "true"
  }, first && isUrl ? /*#__PURE__*/React.createElement("img", {
    src: first,
    alt: "",
    loading: "lazy",
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'contain',
      display: 'block'
    }
  }) : Glyph ? /*#__PURE__*/React.createElement(Glyph, {
    category: device.category,
    size: 22
  }) : null);
}
function YcStatusBadge({
  kind
}) {
  const st = YC_STATES[kind];
  return /*#__PURE__*/React.createElement("span", {
    className: 'yc-badge ' + st.badgeClass
  }, /*#__PURE__*/React.createElement("span", {
    className: "yc-badge-dot",
    "aria-hidden": "true"
  }), st.label);
}

// The "Change" cell: the same detailed change list the contributor profile
// shows — a line per field (verb + noun, with a before→after chip where the
// value is a short scalar), plus the human summary on a live edit. Capped so
// a big edit doesn't blow out the row.
function YcChangeSummary({
  item
}) {
  const list = item.changes || [];
  const Line = window.EditChangeLine;
  if (!list.length) {
    return item.summary ? /*#__PURE__*/React.createElement("span", {
      className: "yct-change-summary"
    }, item.summary) : /*#__PURE__*/React.createElement("span", {
      className: "yct-muted"
    }, "-");
  }
  const MAX = 3;
  const shown = list.slice(0, MAX);
  const extra = list.length - shown.length;
  return /*#__PURE__*/React.createElement("div", {
    className: "yct-change"
  }, item.summary ? /*#__PURE__*/React.createElement("span", {
    className: "yct-change-summary"
  }, item.summary) : null, Line ? /*#__PURE__*/React.createElement("ul", {
    className: "ce-change-list yct-change-list"
  }, shown.map((c, i) => /*#__PURE__*/React.createElement(Line, {
    key: i,
    change: c
  })), extra > 0 ? /*#__PURE__*/React.createElement("li", {
    className: "yct-change-more"
  }, "+", extra, " more field", extra === 1 ? '' : 's') : null) : null);
}

// The review meta line (sits under the last-activity time): the PR number and
// the comment count, as plain text in one matching muted style. No links here —
// the time above is the click target. The PR icon is dropped; the comment
// keeps its bubble glyph.
function YcReviewCell({
  item
}) {
  if (!item.comments || item.comments <= 0) return null;
  return /*#__PURE__*/React.createElement("span", {
    className: "yct-review"
  }, /*#__PURE__*/React.createElement("span", {
    className: "yct-comments",
    title: item.comments + ' comment' + (item.comments === 1 ? '' : 's') + ' on the review'
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "comment",
    size: 12
  }), item.comments));
}

// Last-activity cell: the relative time only. The comment count now lives in
// the actions cell, just left of the ↗ open-on-GitHub icon, so the review meta
// sits next to the link it relates to. The whole row still opens the PR.
function YcUpdated({
  item
}) {
  const rel = window.relTime ? window.relTime(item.at) : item.at;
  const full = window.formatDateTime ? window.formatDateTime(item.at) : item.at;
  return /*#__PURE__*/React.createElement("time", {
    dateTime: item.at,
    title: full
  }, rel);
}

// Action per state. Drafts get a single borderless discard (trash) icon — the
// row itself opens the editor, so there's no separate Continue button.
// Submitted changes (pending / live / declined) live on GitHub as a pull
// request: they show the comment count (when `showComments`) followed by an
// external-link glyph, so it's clear — before the click — that opening the row
// leaves the site for GitHub rather than opening the in-app editor the way a
// draft does. `showComments` is set on the desktop table only; the mobile card
// shows the comment count on its own meta line, so it stays off there.
function YcActionCell({
  item,
  onDiscard,
  showComments
}) {
  if (item.kind === 'draft') {
    return /*#__PURE__*/React.createElement("div", {
      className: "yct-actions"
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "yct-discard",
      "aria-label": "Discard draft",
      onClick: e => {
        e.stopPropagation();
        onDiscard(item.deviceId);
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: "trash",
      size: 15
    })));
  }
  const comments = showComments ? /*#__PURE__*/React.createElement(YcReviewCell, {
    item: item
  }) : null;
  if (item.pr) {
    return /*#__PURE__*/React.createElement("div", {
      className: "yct-actions"
    }, comments, /*#__PURE__*/React.createElement("a", {
      className: "yct-open-pr",
      href: YC_PR_BASE + item.pr,
      target: "_blank",
      rel: "noopener",
      title: "Opens the review on GitHub",
      "aria-label": "Open the review on GitHub",
      onClick: e => e.stopPropagation()
    }, /*#__PURE__*/React.createElement(Icon, {
      name: "open",
      size: 15
    })));
  }
  return comments ? /*#__PURE__*/React.createElement("div", {
    className: "yct-actions"
  }, comments) : null;
}

// Sortable column header.
function YcTh({
  id,
  label,
  sort,
  setSort
}) {
  const active = sort.key === id;
  const onClick = () => setSort(active ? {
    key: id,
    dir: sort.dir === 'asc' ? 'desc' : 'asc'
  } : {
    key: id,
    dir: id === 'updated' ? 'desc' : 'asc'
  });
  return /*#__PURE__*/React.createElement("th", {
    className: 'yct-th' + (active ? ' is-sorted' : ''),
    "aria-sort": active ? sort.dir === 'asc' ? 'ascending' : 'descending' : 'none'
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "yct-th-btn",
    onClick: onClick
  }, label, /*#__PURE__*/React.createElement("span", {
    className: 'yct-caret' + (active ? ' is-on' : '') + (active && sort.dir === 'asc' ? ' is-asc' : ''),
    "aria-hidden": "true"
  })));
}

// ── Submit-all dialog. Centred modal on desktop, slide-up bottom sheet on
// mobile/tablet — mirrors SubmitDialog / ChangesDialog so review surfaces feel
// alike. ──
function YcSubmitModal({
  drafts,
  totalChanges,
  onClose,
  onSubmit
}) {
  const [comment, setComment] = React.useState('');
  const user = window.useCurrentUser && window.useCurrentUser() || null;
  const needsContact = window.hasNotificationMethod ? !window.hasNotificationMethod(user) : !(user && (user.email || user.gh));
  const [isSheet, setIsSheet] = React.useState(() => typeof window !== 'undefined' && window.matchMedia('(max-width: 1023px)').matches);
  React.useEffect(() => {
    const mq = window.matchMedia('(max-width: 1023px)');
    const onChange = e => setIsSheet(e.matches);
    mq.addEventListener ? mq.addEventListener('change', onChange) : mq.addListener(onChange);
    return () => {
      mq.removeEventListener ? mq.removeEventListener('change', onChange) : mq.removeListener(onChange);
    };
  }, []);
  const panelRef = React.useRef(null);
  const [closing, setClosing] = React.useState(false);
  const [dragY, setDragY] = React.useState(0);
  const closeAnimated = React.useCallback(() => {
    if (!isSheet) {
      onClose();
      return;
    }
    const h = panelRef.current?.offsetHeight ?? 600;
    setClosing(true);
    setDragY(h);
    setTimeout(onClose, 200);
  }, [isSheet, onClose]);
  React.useEffect(() => {
    document.body.classList.add('modal-open');
    const onKey = e => {
      if (e.key === 'Escape') closeAnimated();
    };
    document.addEventListener('keydown', onKey);
    return () => {
      document.body.classList.remove('modal-open');
      document.removeEventListener('keydown', onKey);
    };
  }, [closeAnimated]);
  const n = drafts.length;
  const title = 'Submit your edits';
  const body = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("ul", {
    className: "ce-tray-confirm-list"
  }, drafts.map(it => /*#__PURE__*/React.createElement("li", {
    key: it.deviceId,
    className: "ce-tray-confirm-item"
  }, /*#__PURE__*/React.createElement(YcTile, {
    device: it.device
  }), /*#__PURE__*/React.createElement("span", {
    className: "ce-tray-confirm-body"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-name"
  }, it.device.name), /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-mfr"
  }, it.changes.length, " edit", it.changes.length === 1 ? '' : 's'))))), needsContact && /*#__PURE__*/React.createElement("div", {
    className: "ce-notify-warning",
    role: "note"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "info",
    size: 16
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("b", null, "No notification method set up."), " Add a GitHub account or an email so we can tell you when these are reviewed.", ' ', /*#__PURE__*/React.createElement("a", {
    href: "#/settings/notifications",
    onClick: onClose
  }, "Set one up"), ".")), /*#__PURE__*/React.createElement("label", {
    className: "ce-field ce-tray-comment",
    htmlFor: "yc-submit-comment"
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-field-label"
  }, "Comment"), /*#__PURE__*/React.createElement("textarea", {
    id: "yc-submit-comment",
    className: "ce-textarea",
    rows: 3,
    placeholder: "What did you change and why? Added to every review.",
    value: comment,
    onChange: e => setComment(e.target.value)
  })), /*#__PURE__*/React.createElement("p", {
    className: "ce-auth-foot ce-submit-auth-foot"
  }, "Each device is sent for review on GitHub before going live."));
  if (isSheet) {
    return ReactDOM.createPortal(/*#__PURE__*/React.createElement("div", {
      className: 'sheet-backdrop' + (closing ? ' is-closing' : ''),
      onClick: closeAnimated,
      role: "presentation"
    }, /*#__PURE__*/React.createElement("div", {
      className: "sheet-panel yc-submit-sheet",
      ref: panelRef,
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": "yc-submit-title",
      onClick: e => e.stopPropagation(),
      style: {
        transform: dragY ? `translateY(${dragY}px)` : undefined,
        transition: 'transform 200ms cubic-bezier(.2,.0,.2,1)'
      }
    }, /*#__PURE__*/React.createElement("header", {
      className: "sheet-head"
    }, /*#__PURE__*/React.createElement("div", {
      className: "sheet-head-row"
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "sheet-back",
      onClick: closeAnimated,
      "aria-label": "Cancel"
    }, /*#__PURE__*/React.createElement(Icon, {
      name: "x",
      size: 18
    })), /*#__PURE__*/React.createElement("h2", {
      id: "yc-submit-title",
      className: "sheet-title"
    }, title))), /*#__PURE__*/React.createElement("div", {
      className: "sheet-body"
    }, /*#__PURE__*/React.createElement("div", {
      className: "ce-publish-sheet-pad"
    }, body)), /*#__PURE__*/React.createElement("footer", {
      className: "sheet-foot"
    }, /*#__PURE__*/React.createElement("div", {
      className: "ce-confirm-actions ce-confirm-actions-sheet"
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "btn btn-primary",
      onClick: onSubmit
    }, "Submit for review"), /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "btn btn-ghost",
      onClick: closeAnimated
    }, "Cancel"))))), document.body);
  }
  return ReactDOM.createPortal(/*#__PURE__*/React.createElement("div", {
    className: "modal-backdrop",
    onMouseDown: e => {
      if (e.target === e.currentTarget) onClose();
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "modal-dialog ce-publish-dialog yc-submit-modal",
    onClick: e => e.stopPropagation(),
    role: "dialog",
    "aria-modal": "true",
    "aria-labelledby": "yc-submit-title"
  }, /*#__PURE__*/React.createElement("header", {
    className: "modal-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "modal-head-row"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "modal-close",
    onClick: onClose,
    "aria-label": "Close"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "x",
    size: 18
  })), /*#__PURE__*/React.createElement("h2", {
    id: "yc-submit-title"
  }, title))), /*#__PURE__*/React.createElement("div", {
    className: "modal-body"
  }, body), /*#__PURE__*/React.createElement("footer", {
    className: "modal-foot"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ce-confirm-actions ce-confirm-actions-left"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn btn-primary",
    onClick: onSubmit
  }, "Submit for review"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn btn-ghost",
    onClick: onClose
  }, "Cancel"))))), document.body);
}

// ── The page. ──
function YourChanges() {
  const user = window.useCurrentUser && window.useCurrentUser() || null;
  window.useDrafts && window.useDrafts(); // re-render on draft change
  window.usePending && window.usePending('__you__'); // re-render on pending change
  window.useNotifs && window.useNotifs(); // re-render on notif change

  // Open on Drafts when there are any (that's where you act), otherwise on All.
  const [tab, setTab] = React.useState(() => window.draftDeviceCount && window.draftDeviceCount() > 0 ? 'draft' : 'all'); // draft|all|pending|live|declined
  const [sort, setSort] = React.useState({
    key: 'updated',
    dir: 'desc'
  });
  const [query, setQuery] = React.useState('');
  const [discardId, setDiscardId] = React.useState(null);
  const [submitOpen, setSubmitOpen] = React.useState(false);
  const [flash, setFlash] = React.useState(null);
  const [searchOpen, setSearchOpen] = React.useState(false);
  const searchRef = React.useRef(null);

  // Sliding accent rail for the folder list (desktop), mirroring the Settings
  // rail: a 2px line resting on the active folder, animating to the hovered one.
  const railListRef = React.useRef(null);
  const folderRefs = React.useRef([]);
  const rootRef = React.useRef(null);
  // Pin the folder rail just below the sticky top nav. The nav's height differs
  // by breakpoint (one row on desktop, two on tablet), so measure it into a CSS
  // var the rail's sticky `top` reads from.
  React.useEffect(() => {
    const setNavH = () => {
      const nav = document.querySelector('.appnav');
      const h = nav ? Math.round(nav.getBoundingClientRect().height) : 72;
      if (rootRef.current) rootRef.current.style.setProperty('--yct-nav-h', h + 'px');
    };
    setNavH();
    window.addEventListener('resize', setNavH);
    return () => window.removeEventListener('resize', setNavH);
  }, []);
  const [navHover, setNavHover] = React.useState(null);
  const [rail, setRail] = React.useState({
    y: 0,
    h: 0,
    ready: false
  });
  const [railAnimate, setRailAnimate] = React.useState(false);
  const RAIL_ORDER = ['draft', 'all', 'pending', 'live', 'declined'];
  const activeIdx = Math.max(0, RAIL_ORDER.indexOf(tab));
  const measureRail = React.useCallback(() => {
    const idx = navHover != null ? navHover : activeIdx;
    const el = folderRefs.current[idx];
    if (el) setRail({
      y: el.offsetTop,
      h: el.offsetHeight,
      ready: true
    });
  }, [navHover, activeIdx]);
  React.useLayoutEffect(() => {
    measureRail();
  }, [measureRail]);
  React.useEffect(() => {
    const id = requestAnimationFrame(() => setRailAnimate(true));
    return () => cancelAnimationFrame(id);
  }, []);
  React.useEffect(() => {
    window.addEventListener('resize', measureRail);
    return () => window.removeEventListener('resize', measureRail);
  }, [measureRail]);
  React.useEffect(() => {
    if (searchOpen && searchRef.current) searchRef.current.focus();
  }, [searchOpen]);
  if (!user) {
    return /*#__PURE__*/React.createElement("div", {
      className: "container",
      style: {
        padding: '80px 16px',
        textAlign: 'center'
      },
      "data-screen-label": "Your edits (signed out)"
    }, /*#__PURE__*/React.createElement("h1", {
      style: {
        marginBottom: 12
      }
    }, "You're signed out."), /*#__PURE__*/React.createElement("p", {
      className: "muted"
    }, "Sign in to see the edits you have in flight."), /*#__PURE__*/React.createElement("a", {
      className: "btn btn-primary",
      href: "#/signin"
    }, "Sign in"));
  }
  const handle = window.CURRENT_USER;

  // Attach PR comment metadata to any item that carries a PR.
  const withMeta = it => {
    const m = ycPrMeta(it.pr);
    return {
      ...it,
      comments: m ? m.count : 0,
      unanswered: !!(m && m.unanswered)
    };
  };

  // ── Gather the four states into one normalized feed. ──
  const drafts = window.gatherDrafts && window.gatherDrafts() || [];
  const draftsMap = window.allDrafts && window.allDrafts() || {};
  const draftItems = drafts.map(it => {
    const rich = ycRichChanges(it.device, (draftsMap[it.deviceId] || {}).fields);
    return {
      key: 'draft-' + it.deviceId,
      kind: 'draft',
      deviceId: it.deviceId,
      device: it.device,
      at: it.at,
      changes: rich.length ? rich : it.changes,
      comments: 0,
      unanswered: false
    };
  });
  const contrib = window.gatherContributions && window.gatherContributions(handle) || {
    live: [],
    pending: []
  };
  const pendingItems = (contrib.pending || []).map(it => {
    // Pull the submitted value for each field so the row can show before→after,
    // the same way a draft or a live edit does.
    const p = window.getPending && window.getPending(it.deviceId) || {};
    const vmap = {};
    (it.changes || []).forEach(c => {
      if (p[c.field]) vmap[c.field] = p[c.field].value;
    });
    const rich = ycRichChanges(it.device, vmap);
    return withMeta({
      key: 'pending-' + it.deviceId,
      kind: 'pending',
      deviceId: it.deviceId,
      device: it.device,
      at: it.at,
      pr: it.pr,
      changes: rich.length ? rich : it.changes
    });
  });
  const liveItems = (contrib.live || []).map((it, i) => withMeta({
    key: 'live-' + it.deviceId + '-' + i,
    kind: 'live',
    deviceId: it.deviceId,
    device: it.device,
    at: it.at,
    pr: it.pr,
    summary: it.summary,
    changes: it.changes
  }));

  // Declined edits live only in the notification store (no live record).
  const devices = typeof window !== 'undefined' && window.DEVICES || [];
  const notifs = window.loadNotifs && window.loadNotifs() || [];
  const declinedItems = notifs.filter(n => n.type === 'declined').map(n => withMeta({
    key: 'declined-' + n.id,
    kind: 'declined',
    deviceId: n.deviceId,
    device: devices.find(x => x.id === n.deviceId) || {
      id: n.deviceId,
      name: n.device,
      manufacturer: '',
      category: ''
    },
    at: n.ts,
    pr: n.pr,
    changes: n.changes || []
  }));
  const all = [...draftItems, ...pendingItems, ...liveItems, ...declinedItems];
  const counts = {
    // All is every submitted edit; drafts are their own folder and never roll up
    // into All (they are unsubmitted and account-only).
    all: pendingItems.length + liveItems.length + declinedItems.length,
    draft: draftItems.length,
    pending: pendingItems.length,
    live: liveItems.length,
    declined: declinedItems.length
  };
  const totalDraftChanges = draftItems.reduce((n, it) => n + it.changes.length, 0);

  // Filter by folder, then by the device/maker text query. All shows every
  // submitted edit but excludes drafts; the Drafts folder shows only drafts.
  let rows = all.slice();
  if (tab === 'all') rows = rows.filter(it => it.kind !== 'draft');else rows = rows.filter(it => it.kind === tab);
  const q = query.trim().toLowerCase();
  if (q) rows = rows.filter(it => ((it.device.name || '') + ' ' + (it.device.manufacturer || '')).toLowerCase().includes(q));
  rows.sort((a, b) => {
    let cmp = 0;
    if (sort.key === 'updated') cmp = new Date(a.at) - new Date(b.at);else if (sort.key === 'device') cmp = (a.device.name || '').localeCompare(b.device.name || '');else if (sort.key === 'status') cmp = YC_STATES[a.kind].order - YC_STATES[b.kind].order;
    return sort.dir === 'asc' ? cmp : -cmp;
  });

  // Drafts first (where you act), then Submitted with its lifecycle states
  // nested underneath as indented sub-filters. Per-row status badges still
  // distinguish the states inside any view.
  const RAIL = [{
    id: 'draft',
    label: 'Drafts'
  }, {
    id: 'all',
    label: 'Submitted',
    groupStart: true
  }, {
    id: 'pending',
    label: 'Awaiting review',
    sub: true
  }, {
    id: 'live',
    label: 'Live',
    sub: true
  }, {
    id: 'declined',
    label: 'Declined',
    sub: true
  }];
  // Whether a state nested under Submitted is the active view, so the parent
  // "Submitted" can read as the current group even without the accent rail.
  const submittedSub = tab === 'pending' || tab === 'live' || tab === 'declined';
  const showFilter = all.length > 1;
  const activeTabLabel = (RAIL.find(t => t.id === tab) || {}).label || 'Your edits';
  const doSubmitAll = () => {
    const draftsMap = window.allDrafts && window.allDrafts() || {};
    const devicesCount = draftItems.length;
    const changesCount = totalDraftChanges;
    draftItems.forEach(it => {
      const entry = draftsMap[it.deviceId] || {};
      const fields = entry.fields || {};
      const sources = entry.sources || null;
      const entries = {};
      Object.keys(fields).forEach(k => {
        entries[k] = {
          value: fields[k]
        };
        if (sources && sources[k]) entries[k].source = sources[k];
      });
      if (window.addPendingEdit) window.addPendingEdit(it.deviceId, entries);
      if (window.openPullRequest) window.openPullRequest(it.device);
      if (window.clearDraft) window.clearDraft(it.deviceId);
    });
    setSubmitOpen(false);
    setTab('all');
    setFlash({
      devices: devicesCount,
      changes: changesCount
    });
  };
  const rel = iso => window.relTime ? window.relTime(iso) : iso;
  const full = iso => window.formatDateTime ? window.formatDateTime(iso) : iso;
  const emptyCopy = {
    all: "You haven't submitted any edits yet. Anything you start is saved under Drafts.",
    draft: 'No drafts. Edits you start but have not submitted collect here.',
    pending: 'Nothing awaiting review right now.',
    live: 'None of your edits are live yet.',
    declined: 'Nothing was sent back.'
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "container yct",
    "data-screen-label": "Your edits",
    ref: rootRef
  }, /*#__PURE__*/React.createElement("div", {
    className: "yct-shell"
  }, /*#__PURE__*/React.createElement("aside", {
    className: "yct-rail"
  }, /*#__PURE__*/React.createElement("nav", {
    className: "yct-rail-list",
    ref: railListRef,
    role: "tablist",
    "aria-label": "Filter your edits",
    onMouseLeave: () => setNavHover(null)
  }, /*#__PURE__*/React.createElement("span", {
    className: "yct-rail-railline",
    "aria-hidden": "true",
    style: {
      transform: `translateY(${rail.y}px)`,
      height: rail.h + 'px',
      opacity: rail.ready ? 1 : 0,
      transition: railAnimate ? undefined : 'none'
    }
  }), RAIL.map((t, i) => {
    // Hide a nested lifecycle filter (Awaiting review / Live /
    // Declined) when it has nothing in it, so the rail only lists
    // states you actually have. Returning null keeps the map index
    // stable, so the accent-rail measurement stays aligned.
    if (t.sub && counts[t.id] === 0) return null;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: t.id
    }, t.groupStart ? /*#__PURE__*/React.createElement("span", {
      className: "yct-rail-sep",
      "aria-hidden": "true"
    }) : null, /*#__PURE__*/React.createElement("button", {
      type: "button",
      role: "tab",
      "aria-selected": tab === t.id,
      ref: el => {
        folderRefs.current[i] = el;
      },
      onMouseEnter: () => setNavHover(i),
      className: 'yct-folder' + (t.sub ? ' yct-folder-sub' : '') + (t.groupStart ? ' yct-folder-group' : '') + (tab === t.id ? ' is-active' : '') + (t.id === 'all' && submittedSub ? ' is-group-active' : ''),
      onClick: () => setTab(t.id)
    }, /*#__PURE__*/React.createElement("span", {
      className: "yct-folder-label"
    }, t.label), /*#__PURE__*/React.createElement("span", {
      className: "yct-folder-count"
    }, counts[t.id])));
  }))), /*#__PURE__*/React.createElement("div", {
    className: "yct-main"
  }, /*#__PURE__*/React.createElement("header", {
    className: "yct-head"
  }, /*#__PURE__*/React.createElement("div", {
    className: "yct-head-text"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow section-eyebrow",
    style: { marginBottom: 8, color: "var(--primary)" }
  }, "Your edits"), /*#__PURE__*/React.createElement("h1", {
    className: "yct-title"
  }, activeTabLabel)), /*#__PURE__*/React.createElement("div", {
    className: "yct-head-actions"
  }, showFilter && /*#__PURE__*/React.createElement("div", {
    className: 'yct-search' + (searchOpen || query ? ' is-open' : '')
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "search",
    size: 14
  }), /*#__PURE__*/React.createElement("input", {
    ref: searchRef,
    className: "yct-search-input",
    type: "text",
    value: query,
    placeholder: "Filter",
    "aria-label": "Filter your edits by device or manufacturer",
    onChange: e => setQuery(e.target.value),
    onFocus: () => setSearchOpen(true),
    onKeyDown: e => {
      if (e.key === 'Escape') {
        setQuery('');
        e.target.blur();
      }
    },
    onBlur: () => {
      if (!query.trim()) setSearchOpen(false);
    }
  }), query && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "yct-search-clear",
    "aria-label": "Clear filter",
    onClick: () => {
      setQuery('');
      if (searchRef.current) searchRef.current.focus();
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "x",
    size: 13
  }))), counts.draft > 0 && /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn btn-primary yct-submit-all",
    onClick: () => {
      setFlash(null);
      setSubmitOpen(true);
    }
  }, `Submit ${counts.draft} draft${counts.draft === 1 ? '' : 's'}`))), flash && /*#__PURE__*/React.createElement("div", {
    className: "yc-flash",
    role: "status"
  }, /*#__PURE__*/React.createElement("span", {
    className: "yc-flash-ico",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "checkcircle",
    size: 18
  })), /*#__PURE__*/React.createElement("div", {
    className: "yc-flash-text"
  }, /*#__PURE__*/React.createElement("b", null, "Sent for review"), /*#__PURE__*/React.createElement("span", null, flash.changes, " edit", flash.changes === 1 ? '' : 's', " across ", flash.devices, " device", flash.devices === 1 ? '' : 's', " ", flash.changes === 1 ? 'is' : 'are', " on the way to our maintainers. We'll let you know as each one is reviewed.")), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "yc-flash-x",
    "aria-label": "Dismiss",
    onClick: () => setFlash(null)
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "x",
    size: 15
  }))), rows.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "yct-empty"
  }, /*#__PURE__*/React.createElement(Icon, {
    name: q ? 'search' : 'checkcircle',
    size: 26
  }), /*#__PURE__*/React.createElement("p", null, q ? 'No edits match “' + query.trim() + '”.' : emptyCopy[tab]), q ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "btn btn-secondary",
    onClick: () => setQuery('')
  }, "Clear filter") : tab === 'all' ? /*#__PURE__*/React.createElement("a", {
    className: "btn btn-secondary",
    href: "#/browse"
  }, "Browse devices") : null) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "yct-tablewrap"
  }, /*#__PURE__*/React.createElement("table", {
    className: "yct-table"
  }, /*#__PURE__*/React.createElement("colgroup", null, /*#__PURE__*/React.createElement("col", {
    className: "yct-col-device"
  }), /*#__PURE__*/React.createElement("col", {
    className: "yct-col-change"
  }), /*#__PURE__*/React.createElement("col", {
    className: "yct-col-status"
  }), /*#__PURE__*/React.createElement("col", {
    className: "yct-col-updated"
  }), /*#__PURE__*/React.createElement("col", {
    className: "yct-col-actions"
  })), /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement(YcTh, {
    id: "device",
    label: "Device",
    sort: sort,
    setSort: setSort
  }), /*#__PURE__*/React.createElement("th", {
    className: "yct-th yct-th-plain"
  }, "Edit"), /*#__PURE__*/React.createElement(YcTh, {
    id: "status",
    label: "Status",
    sort: sort,
    setSort: setSort
  }), /*#__PURE__*/React.createElement(YcTh, {
    id: "updated",
    label: "Last activity",
    sort: sort,
    setSort: setSort
  }), /*#__PURE__*/React.createElement("th", {
    className: "yct-th yct-th-plain yct-th-actions",
    "aria-label": "Actions"
  }))), /*#__PURE__*/React.createElement("tbody", null, rows.map(it => /*#__PURE__*/React.createElement("tr", {
    key: it.key,
    className: 'yct-row' + (it.kind === 'draft' ? ' is-draft' : ''),
    onClick: ycRowClick(it)
  }, /*#__PURE__*/React.createElement("td", {
    className: "yct-td yct-cell-device"
  }, /*#__PURE__*/React.createElement(YctThumb, {
    device: it.device
  }), /*#__PURE__*/React.createElement("span", {
    className: "yct-device-id"
  }, /*#__PURE__*/React.createElement("a", {
    className: "yct-device-name",
    href: '#/device/' + it.deviceId
  }, it.device.name), /*#__PURE__*/React.createElement("span", {
    className: "yct-device-mfr"
  }, it.device.manufacturer))), /*#__PURE__*/React.createElement("td", {
    className: "yct-td yct-cell-change"
  }, /*#__PURE__*/React.createElement(YcChangeSummary, {
    item: it
  })), /*#__PURE__*/React.createElement("td", {
    className: "yct-td yct-cell-status"
  }, /*#__PURE__*/React.createElement(YcStatusBadge, {
    kind: it.kind
  })), /*#__PURE__*/React.createElement("td", {
    className: "yct-td yct-cell-updated"
  }, /*#__PURE__*/React.createElement(YcUpdated, {
    item: it
  })), /*#__PURE__*/React.createElement("td", {
    className: "yct-td yct-cell-actions"
  }, /*#__PURE__*/React.createElement(YcActionCell, {
    item: it,
    onDiscard: setDiscardId,
    showComments: true
  }))))))), /*#__PURE__*/React.createElement("ul", {
    className: "ce-contrib-list yct-cards"
  }, rows.map(it => /*#__PURE__*/React.createElement("li", {
    key: it.key,
    className: 'ce-contrib-row' + (it.kind === 'draft' ? ' is-draft' : ''),
    onClick: ycRowClick(it)
  }, /*#__PURE__*/React.createElement(YcTile, {
    device: it.device
  }), /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-body"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-meta"
  }, /*#__PURE__*/React.createElement("time", {
    className: "ce-contrib-when",
    dateTime: it.at,
    title: window.formatDateTime ? window.formatDateTime(it.at) : it.at
  }, window.relTime ? window.relTime(it.at) : it.at), /*#__PURE__*/React.createElement(YcReviewCell, {
    item: it
  }), /*#__PURE__*/React.createElement(YcStatusBadge, {
    kind: it.kind
  }), /*#__PURE__*/React.createElement(YcActionCell, {
    item: it,
    onDiscard: setDiscardId
  })), /*#__PURE__*/React.createElement("div", {
    className: "ce-contrib-top"
  }, /*#__PURE__*/React.createElement("a", {
    className: "ce-contrib-device",
    href: '#/device/' + it.deviceId
  }, /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-name"
  }, it.device.name), /*#__PURE__*/React.createElement("span", {
    className: "ce-contrib-mfr"
  }, it.device.manufacturer))), /*#__PURE__*/React.createElement(YcChangeSummary, {
    item: it
  })))))))), submitOpen && /*#__PURE__*/React.createElement(YcSubmitModal, {
    drafts: draftItems,
    totalChanges: totalDraftChanges,
    onClose: () => setSubmitOpen(false),
    onSubmit: doSubmitAll
  }), window.ConfirmDialog && /*#__PURE__*/React.createElement(window.ConfirmDialog, {
    open: !!discardId,
    title: "Discard this draft?",
    message: "Your unsubmitted edits to this device will be lost. This can't be undone.",
    confirmLabel: "Discard draft",
    cancelLabel: "Keep draft",
    danger: true,
    onConfirm: () => {
      if (window.clearDraft) window.clearDraft(discardId);
      setDiscardId(null);
    },
    onCancel: () => setDiscardId(null)
  }));
}

// ── Nav widget: a single icon that links to the page, with a drafts badge. ──
function YourChangesNavButton() {
  const user = window.useCurrentUser && window.useCurrentUser() || null;
  window.useDrafts && window.useDrafts();
  if (!user) return null;
  const drafts = window.draftDeviceCount && window.draftDeviceCount() || 0;
  return /*#__PURE__*/React.createElement("span", {
    className: "ce-inbox",
    "data-inc-target": "your-changes"
  }, /*#__PURE__*/React.createElement("a", {
    className: "ce-inbox-btn yc-navlink-btn",
    href: "#/you",
    "aria-label": drafts ? `Your edits, ${drafts} draft${drafts === 1 ? '' : 's'} in progress` : 'Your edits'
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "inbox",
    size: 19
  }), drafts > 0 && /*#__PURE__*/React.createElement("span", {
    className: "ce-inbox-badge",
    "aria-hidden": "true"
  }, drafts > 9 ? '9+' : drafts)));
}
Object.assign(window, {
  YourChanges,
  YourChangesNavButton
});
